#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
bool cmp(ll a, ll b){
  
}
int main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  int n;
  cin >> n;

  ll test = 1e18;

  vector<ll> arr(n + 1, 0);

  for (int i = 1; i <= n; i++)
  {
    cin >> arr[i];
  }
  return 0;
}